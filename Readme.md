git add.
git commit -m "first commit"
git push






react 19
Eliminates the need for manual memoization techniques like useMemo and useCallback, reducing boilerplate and improving performance

preassigned url 
tcs codevita

“It’s the same concept as AWS presigned URLs; the frontend uploads files directly to Cloudinary using a signature generated by the backend, and the backend only stores the

tenstack quary 
post on backgound 


If hideRepostButton is false or not given,
→ !false = true → the button shows.

If hideRepostButton is true,
→ !true = false → the button doesn’t render.